The new numbers make the table more informative: readers can now see that Self-Attn achieves 2.85/3.12 = 91% of its peak, MatMul Accel achieves 13.52/22.83 = 59%, and This Work achieves 2.76/8.00 = 34.5% — which is the lowest efficiency but on the hardest workload (1.2B-param FFN vs DistilBERT-scale single matmul). The contrast is worth discussing if it isn't already.

It must be noted that this 1.2B model has different dimensions, hence compute units cannot be utilized across compute stages. 